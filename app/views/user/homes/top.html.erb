<div class="container">
    <div class="row">
        <div class="col">
            <h1 class="welcome">Welcome</h1>
            <h1 class="welcome">to</h1>
            <h1 class="welcome">Stargazer</h1>
            <h4>
                <% if admin_signed_in? %>
                    管理者としてログインしています
                <% else %>
                    <% if user_signed_in? %>
                        ゲストとしてログインしています
                    <% else %>
                        ゲストとして<%= link_to 'ログイン', user_guest_sign_in_path, method: :post %>する
                    <% end %>
                <% end %>
            </h4>
        </div>
        <div class="col">
            <%= image_tag '073Kazuki17103_TP_V.jpeg', size: '600x400' %>
        </div>
    </div>
</div>

